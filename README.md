#guide-automator-python

# Instruções

*Para instalar as dependências, use **$ pip install -r requirements.txt**, após ter instalado o pip e o python3*

Recomendado trabalhar utilizando uma virtual-env.
Dentro da pasta do projeto:

*$ python3 -m venv .* 
*$ source bin/activate*

